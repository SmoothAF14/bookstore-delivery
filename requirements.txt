# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Data validation
pydantic==2.10.4
pydantic-settings==2.7.0

# Async task scheduling/execution
celery==5.4.0
redis==5.2.1

# HTTP client (tool calls into the Django backend / tracking service)
httpx==0.28.1

# Environment
python-decouple==3.8

# JWT verification — validates access tokens issued by the Django backend
pyjwt==2.10.1

# LLM SDK (OpenRouter, OpenAI-compatible) — delivery is the 3rd AI feature;
# the LLM reviews/adjusts the rule-based classification. Same key as the
# assistant + tracking bots.
openai==1.59.6
