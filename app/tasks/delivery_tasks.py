"""
delivery_tasks.py — Celery tasks for asynchronous delivery processing.

Placeholder scaffolding. Tasks to classify orders and schedule dispatch are to
be implemented and scheduled via Celery beat.
"""

# TODO: @celery task — classify_order(order_id): run the classifier.
# TODO: @celery task — schedule_dispatch(order_id): compute + persist dispatch time.
